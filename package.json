{
  "name": "tide",
  "version": "1.0.0",
  "private": true,
  "description": "Keyboard-first calendar over Google Calendar, on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev --port 8788 --ip 127.0.0.1",
    "predeploy": "node scripts/rehash-csp.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
